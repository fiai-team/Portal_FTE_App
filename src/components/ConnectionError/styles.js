const styles = (theme) => ({
    cloudContainer: {
        "-webkit-align-items": "center",
        "-ms-flex-align": "center",
        alignItems: "center",
        display: "flex",
        justifyContent: "center",
    }
});

export default styles;
